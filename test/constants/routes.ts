/**
 * Hằng số các đường dẫn (Routes) trong ứng dụng
 */
export const APP_ROUTES = {
  DASHBOARD: '/admin',
  LOGIN: '/login',
  // Thêm các route module khác tại đây
  ADMIN: {
    USERS: '/admin/users',
    ROLES: '/admin/roles',
  },
  PM: {
    PROJECTS: '/pm/projects',
  }
};
